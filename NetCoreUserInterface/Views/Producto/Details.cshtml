@model NetCoreUserInterface.Models.Producto

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Producto</h4>
    <hr />
    <div>
        <hr />

        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <img src="@Html.DisplayFor(model => model.image)" />
                </div>
                <div class="col-md-6">
                    <h2 class="name">@Html.DisplayFor(model => model.Name)</h2><br />
                    <p class="description">Descripción: @Html.DisplayFor(model => model.description)</p><br />
                    <label class="price">Precio: $@Html.DisplayFor(model => model.price)</label>
                </div>
            </div>
        </div>
        @Html.ActionLink("Edit Product", "Edit", new { id = Model.IdProduct })
        @Html.ActionLink("Delete Product", "Delete", new { id = Model.IdProduct })
    </div>
    <div>
    </div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
